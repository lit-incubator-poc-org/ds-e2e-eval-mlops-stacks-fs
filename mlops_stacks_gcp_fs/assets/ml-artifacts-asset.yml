# Unity Catalog models are created automatically when logged, no pre-creation needed

# Allow users to read the experiment and the model
common_permissions: &permissions
  permissions:
    - level: CAN_READ
      group_name: users



# Defines model and experiments
resources:
  # Unity Catalog models are created automatically when logged, no need to pre-create them
  # models:
  #   model:
  #     name: ${var.model_name}
  #     <<: *permissions
  #     depends_on:
  #       - resources.jobs.model_training_job.id
  #       - resources.jobs.batch_inference_job.id

  experiments:
    experiment:
      name: ${var.experiment_name}
      <<: *permissions
